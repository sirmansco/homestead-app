---
title: Homestead — SHIPLOG
purpose: Per-merge ship entries (Protos v9.7 §"Review and ship"). Append-only.
---

## Format

```
### YYYY-MM-DD · <PR #> · <one-line title>
**Branch:** <branch> → main (<merge sha>)
**Plan:** <path/to/plan.md>
**What shipped:** (1-2 sentences, bar-tied)
**Verification:** (test path / preview URL / verified-by trailer)
**Follow-ups:** (none, or batch-id of next dependent work)
```

---

### 2026-05-02 · #54 · B5 — Lantern silent-success: notify outcomes + skip logs + AppDataContext error visibility
**Branch:** `fix/launch-b5-lantern-silent-success` → main (`3c4a3a9`)
**Plan:** [docs/plans/launch-audit-fix-batch-05-lantern-silent-success.md](docs/plans/launch-audit-fix-batch-05-lantern-silent-success.md)
**What shipped:** Closes synthesis Theme E (L13 blocks-launch + L16 should-fix + L29 should-fix). Spec NN #4 ("No fire-and-forget notifications. Every send is awaited; failures are logged. No `.catch(() => {})`") becomes operationally true in `lib/notify.ts` and `app/context/AppDataContext.tsx` for the first time — the constitutional letter held pre-B5 (every push call had try/catch around await), but the spirit didn't (silent-skip paths returned without ever reaching the catch, and the synthesized `{ sent: recipients, eligible: recipients }` return shape collapsed `vapid_not_configured` / zero subs / partial failure all into "sent = recipients"). Server: `notifyBellRing` and `notifyNewShift` now return discriminated `NotifyResult` (`delivered | partial | no_recipients | vapid_missing | push_error`) sourced from the real `PushResult` via a small `pushResultToNotify` mapper; `/api/bell` and `/api/shifts` POST surface `notify` instead of `notifySent`/`notifyEligible`. Every silent-skip early return in `lib/notify.ts` emits a structured `notify_*_skip` JSON log line (event + reason + context, shape matching `lib/push.ts:99` and B4's `bell_cron`). Client: `ScreenLantern` and `ScreenPost` discriminate on `notify.kind` for the user-visible warning (new `partial` branch added). `ScreenSettings.permState` extended with `granted_unregistered` (browser allowed push but `/api/push/subscribe` failed) and `failed` (subscribe threw) — the green "Push notifications enabled" chip no longer renders when the server-side registration failed. `AppDataContext.tsx` three bare `catch {}` blocks (bell, shifts, village polling) replaced with `Sentry.captureException(err, { tags: { source: 'appdata:*' } })` + `console.warn` with stable tag (Sentry already in every client bundle via `app/error.tsx` / `sentry.client.config.ts`; tree-shakable re-import, zero new bundle weight). **Plan §1 pressure-test refuted user's Option A default** (PushResult passthrough) in favor of Option B (discriminated NotifyResult): client needs discrimination either way; PushResult doesn't capture "no recipients found" cleanly; future L17/L18 reasons extend the union additively. **Plan §4 reduced** the proposed five-state PermState refactor to two new states; full split would require a 4×4 unreachable-cell matrix and a registration-state context (Fragile area §1, deferred). **Stage 2 review found a real silent-success bug** that B5's main commit had introduced: `pushResultToNotify` mis-mapped `attempted: 0` (targeted caregiver opted-in but with zero push subscription rows) as `kind: 'delivered'` — the exact regression class L13 closes. Three-line guard added in fixup `00e6c39` returning `{ kind: 'push_error', error: 'no_subscriptions' }` instead, plus a regression test that goes red when reverted. Stage 2 also caught that the `'failed'` PermState branch (50 lines of UI added) was unreachable because `requestPushPermission`'s catch-error path returned a raw error string that didn't match the `subscribe_api_*`/`vapid_key_missing` prefixes; `handleEnableNotifications` reworked to route the catch-error case explicitly. Both Stage-2 fixes shipped in the same fixup before merge. **Out-of-scope held:** L17/L18/L19 (Theme G), L25/L26 (Theme I), L27 (Theme J), L8 (typed auth helpers); two service-worker `.catch(() => {})` patterns at `ScreenPost.tsx:45` and `HomesteadApp.tsx:236` flagged for a future fragile-area cleanup batch.
**Verification:** [tests/notify-outcomes.test.ts](tests/notify-outcomes.test.ts) — 10 cases (server-side spies on returned `kind` AND structured `console.log` JSON). [tests/notify-skip-logs.test.ts](tests/notify-skip-logs.test.ts) — 6 cases (L16 paths in void-returning notify functions). [tests/appdata-context-error-visibility.test.ts](tests/appdata-context-error-visibility.test.ts) — 6 cases (source-grep matching codebase pattern; no RTL installed and plan §7 forbids new deps). Stage 1 spec-reviewer (fresh context) PASS, 9/9 questions; no constitutional regressions detected. Stage 2 code-reviewer (fresh context) PASS-WITH-NOTES, 9/9 questions with 4 notes; 3 closed in fixup `00e6c39` (no_subscriptions guard, reachable `'failed'` PermState, Sentry tag-stability comment), 1 deferred (`notify_threw` vs `push_error` discriminator — current `error: 'notify_threw'` string is greppable, optional follow-up). **Falsifiability proven** for every fix per the 2026-05-02 "spy on the gate" lesson: reverted `notifyBellRing`'s `pushResultToNotify` call to the synthesized shape — `vapid_missing` and `partial` tests went red, restored; removed one `logSkip` call — `creator_opted_out` test went red, restored; reverted bell catch in `AppDataContext` to bare `catch {}` — bare-catch detector + bell-Sentry-tag + bell-console.warn tests went red, restored; reverted Stage 2 `attempted: 0` guard — new `no_subscriptions` test went red, restored. **Verification gates:** `grep -n "{ sent: " lib/notify.ts app/api/bell/route.ts app/api/shifts/route.ts` — no matches (L13 root cause shape gone). `grep -n "} catch {" app/context/AppDataContext.tsx` — no matches (L29 root cause gone). `grep -c "logSkip" lib/notify.ts` — 25 (≥11 floor). Full suite: 24 files / 206/206 tests pass (was 184; +22 new). Lint identical to main: `27 problems (6 errors, 21 warnings)` — verified by `git stash && npm run lint`; zero new lint problems. `db:doctor` clean. CI: Vercel deploy passed on first poll (no fixup deploys needed beyond the Stage-2 fixup which itself shipped pre-merge).
**Follow-ups:** **`notify_threw` discriminator** (Stage 2 note 4, deferred) — current bell/shifts route initial-state default `{ kind: 'push_error', recipients: 0, error: 'notify_threw' }` parses fine but a dedicated `reason: 'notify_threw'` field on `push_error` would slice cleaner in logs; ~5-line follow-up. **`PushRegistrar` ↔ `ScreenSettings` registration-state context** (plan Fragile area §1) — closes the asymmetry where the registrar's mount-time subscribe failure isn't visible in Settings; needs a shared context, worth a separate batch when push-related UI gets revisited (Theme G adjacency). **Recurrence-shifts notify** (plan Fragile area §3) — only the first generated row's `notifyNewShift` is captured today (pre-existing intent: spec calls for one notification per series, not per occurrence); future enhancement could return `notify: NotifyResult[]` for response-shape honesty. **Service-worker `.catch(() => {})`** at `app/components/ScreenPost.tsx:45` and `app/components/HomesteadApp.tsx:236` (post-install side-effects) — low-risk; defer to a fragile-area cleanup batch. Theme E closes. **Theme G (push subscription correctness + cost — L17 push pruning + L18 unique index + L19 deep link)** is the next sequenced batch per synthesis line 337.

### 2026-05-02 · #52 · B4 — Cron wiring + bounded escalation handler + bells escalation index
**Branch:** `fix/launch-b4-cron-escalation` → main (`c13e848`)
**Plan:** [docs/plans/launch-audit-fix-batch-04-cron-escalation.md](docs/plans/launch-audit-fix-batch-04-cron-escalation.md)
**What shipped:** Closes synthesis L14 (cron not wired in `vercel.json`) + L15 (unbounded backlog blast). Spec contract at `docs/specs/homestead.md:82` ("If no positive response within 5 minutes → escalate to sitter tier") is now operational for the first time — the route existed but was never scheduled. `vercel.json` adds `crons: [{ path: "/api/bell/cron", schedule: "*/1 * * * *" }]` and `functions: { "app/api/bell/cron/route.ts": { maxDuration: 60 } }`. Cron handler refactored: `LIMIT 50` on the due-bell SELECT, hand-rolled `runWithConcurrency` cap 10 (no `p-limit` dep — zero codebase precedent for a concurrency limiter; ~12-line worker pool, mirrors `Promise.allSettled` return shape), structured `bell_cron` JSON log on every tick (success or failure) for ops scrape-ability per `lib/push.ts:99` precedent. Failure-detail `console.error` preserved on rejected results. New composite index `idx_bells_status_escalated_created` on `(status, escalated_at, created_at)` matches the cron WHERE column order. Index migration validated the B-snapshots repair: `db:generate` produced a clean single-line `0007` migration with no phantom ALTERs. **Hard prereq:** Vercel project must be on Pro tier (Hobby caps crons at once-daily per BUILD-LESSONS Principle 9 / 2026-04-26 lesson). User confirmed during plan review; deploy succeeded. **Plan deviation flagged:** dropped `eligible_total` from the structured log (would have required a second `SELECT COUNT(*)` per tick). Bound itself is the safety belt; backlog drain is observable via successive ticks reporting `processed=50`.
**Verification:** [tests/bell-cron.test.ts](tests/bell-cron.test.ts) — 9 cases (auth ×4, LIMIT enforcement + structured log ×2, concurrency cap ×1, per-bell failure isolation ×1, empty due-set ×1). Stage 1 spec-reviewer (fresh context) PASS-WITH-NOTES (one finding: original LIMIT test asserted "if 50 rows return, the route processes 50" but did not spy on `.limit()` — a regression dropping `.limit(BATCH_LIMIT)` would have slipped past). Stage 2 code-reviewer (fresh context) PASS-WITH-NOTES; concurrency-cap pressure-tested against (cap removed / cap=1 / serial regression) — all caught. Both reviewer findings addressed in fixup commit `7b1a7d4`: `chain.limit` instrumented as `vi.fn`, LIMIT test now asserts `toHaveBeenCalledWith(50)` AND processes the bounded batch. Falsifiable proof of the regression-test fix: temporarily reverted `.limit(BATCH_LIMIT)` from the route, observed the test fail with `expected "vi.fn()" to be called with arguments: [ 50 ]`, restored. Concurrency-cap test stability rationale (`>1 AND ≤10` is a *bound*, not an exact value — CI scheduler jitter cannot violate a bound) folded into the test's leading comment per Stage 2 note #4. Full suite: 21 files / 184/184 tests pass (was 175 — +9 new). CI: Vercel deploy passed twice (initial + fixup); migrate ran `0007_bells_escalation_index.sql` against prod successfully.
**Follow-ups:** **Pro-tier billing dependency** (Stage 2 note #2) — this batch flips a billing requirement; future operators should know without grep'ing PRs. Captured here. **Staging EXPLAIN check** on `idx_bells_status_escalated_created` — Postgres b-tree usage for `IS NULL` predicates is version-dependent; pre-launch staging should confirm `Index Scan` not `Seq Scan` (plan §"Fragile area §3"). Partial-index `WHERE escalated_at IS NULL` is the documented fallback. **db:doctor environment discrepancy** noted — PR description claimed "1 expected pending warning" but Stage 2 reviewer's local DB had 0007 already applied (zero warnings). Not a fabrication; just env state drift. **Per-bell `console.error` in `lib/bell-escalation.ts:26` lacks structured event field** — pre-existing CLAUDE.md fragile-area #4 finding; defer to L16 batch. Theme F closes. Theme E (lantern silent-success — L13 + L29 + L16) is the next sequenced batch per synthesis line 335.

### 2026-05-02 · #50 · B-snapshots — Reconstruct missing drizzle snapshots + ship pending `village_group` default ALTER
**Branch:** `fix/migrations-snapshot-repair` → main (`b3fee55`)
**Plan:** [docs/plans/migrations-snapshot-repair.md](docs/plans/migrations-snapshot-repair.md)
**What shipped:** Closes the snapshot-drift half of synthesis L11 + ships a genuinely-missing default migration. Snapshot files for `0001`, `0004`, `0005` were never committed; every `db:generate` since `0004` had been silently bundling those migrations' enum/default ALTERs into the next .sql output, which would have failed the next deploy at `ALTER TYPE ADD VALUE 'covey'` (no `IF NOT EXISTS`, value already in prod). Reconstructed three snapshots from each migration's SQL, repointed `0002.prevId` from `0000.id` to `0001.id` (kit rejects DAGs — empirically discovered during build, original plan hypothesis was wrong), and shipped `0006_village_group_default.sql` with the two `SET DEFAULT 'covey'` ALTERs that have been pending since the rebrand (`0005` only UPDATE'd rows; the column-default rename was committed to schema.ts but never migrated). Doctor extended with check #8 (warn-mode): every `<tag>.sql` has a matching `meta/<idx>_snapshot.json`. Discovered mid-B4 build phase; B4 now unblocked — next `db:generate` produces a clean single-line `0007_<random>.sql` for the bells index.
**Verification:** [tests/migrations-snapshot.test.ts](tests/migrations-snapshot.test.ts) — 6 cases (snapshot existence, prevId chain resolution, no-DAG, post-state spot-checks for 0001/0004/0006). Stage 1 spec-reviewer (fresh context) PASS. Stage 2 code-reviewer (fresh context) PASS-WITH-NOTES (two plan-doc defects: stale DAG-tolerance hypothesis, wrong doctor draft code); both addressed in fixup commit `780d40d` before merge. Falsifiable proof: `npm run db:generate` reports "No schema changes, nothing to migrate". Full suite: 20 files / 175/175 tests pass (was 169 — +6 new). `npm run db:doctor` clean. CI: Vercel deploy passed twice (initial + fixup).
**Follow-ups:** Promote doctor check #8 from warn → error in a follow-up batch (~3-line edit; intentional gradual-rollout per plan §"Pressure-test §3"). Resume B4 (cron escalation) on rebased branch — index migration regenerates clean atop main. Synthesis L11's raw-script audit (`scripts/migrate-*.ts`) remains a separate batch.

### 2026-05-02 · #43 · B1 — `requireHouseholdAdmin()` + admin authority migration
**Branch:** `fix/launch-b1-admin-authority` → main (`062a245`)
**Plan:** [docs/plans/launch-audit-fix-batch-01-admin-authority.md](docs/plans/launch-audit-fix-batch-01-admin-authority.md)
**What shipped:** Closes synthesis L4. Single `requireHouseholdAdmin()` helper now gates household profile PATCH, member PATCH/DELETE, and admin transfer; `user.role !== 'parent'` ad-hoc checks deleted; divergent free-text errors collapsed to canonical `{ error: 'no_access' }` 403. `NotAdminError` lives in `lib/api-error.ts` (re-exported from `lib/auth/household.ts`) so `authError()`'s `instanceof` check stays resolvable across the test surface.
**Verification:** [tests/auth-access-household-admin.test.ts](tests/auth-access-household-admin.test.ts) — 19 cases. Gate-logic block exercises the real `requireHouseholdAdmin` against stubbed `auth()` + `db.select` (`vi.importActual`). Route matrix asserts admin → 200, parent-without-isAdmin → 403 `no_access`, unauthenticated → 401 `not_signed_in`, non-member → 409 `no_household` for each migrated route. CI: Vercel deploy passed.
**Follow-ups:** B2 (village CRUD admin gate L2 + village invite role allowlist L3 + notification preferences identity scoping L5) — unblocked.

### 2026-05-02 · #48 · B3 — Member tombstone service + admin Clerk-membership drop parity
**Branch:** `fix/launch-b3-member-tombstone` → main (`e2588ea`)
**Plan:** [docs/plans/launch-audit-fix-batch-03-soft-delete-fk.md](docs/plans/launch-audit-fix-batch-03-soft-delete-fk.md)
**What shipped:** Closes synthesis L9 + L2's delete-safety half + B2 SHIPLOG follow-up. New `lib/users/tombstone.ts` is the canonical per-household user-removal path: detects authored history on the two `ON DELETE restrict` FKs (`shifts.createdByUserId`, `bells.createdByUserId`) and either hard-deletes (zero history) or anonymizes in place using the canonical `[deleted]` placeholder pattern from `account/route.ts` (per spec NN #16b). Wraps in `db.transaction` with FK-race fall-through to anonymize. Pre-cleanup matches `account/route.ts` parity: nulls `claimedByUserId`, cancels future-authored shifts, and on the anonymize branch deletes `pushSubscriptions`, `caregiverUnavailability`, and pending `familyInvites` (Stage 2 review note — closed). Three caller routes migrated: `village/leave`, `village/route.ts` DELETE-adult, `household/members/[id]` DELETE. Admin village-DELETE-adult now drops Clerk org membership in parity with `members/[id]` (B2 SHIPLOG follow-up — closed); both admin routes surface `clerkDropped: boolean` in the response and structured log so Clerk-side failures aren't invisible (`account/route.ts:148-156` parity, Stage 2 review note — closed). Bare `catch {}` on the existing `members/[id]` Clerk drop replaced with `console.error` (Hard Rule #3). `account/route.ts:127` migration to the service deferred (Pressure-test §5 in plan — multi-household loop + own Clerk-user-deletion conflicts with service's per-household scoping).
**Verification:** [tests/user-tombstone.test.ts](tests/user-tombstone.test.ts) — 13 cases (5 service unit including FK-race fallthrough, 8 route integration covering all three callers + Clerk-drop success/failure + anonymize-branch). Stage 1 spec-reviewer (fresh context) graded against spec NN #16b/#3 and the data-integrity bar — PASS. Stage 2 code-reviewer (fresh context) pressure-tested Principle 6 ordering, postgres-js error surface for the FK-race catch (verified live, not dead code), unique-constraint freed by anonymize's `clerkUserId` rewrite — PASS-WITH-NOTES; all 3 notes closed in fixup commit (`2bcb277`) before merge. Full suite: 19 files / 169 tests pass. Grep gate: `(db|tx)\.delete\(users\)` returns only `lib/users/tombstone.ts:58` (hard-delete branch) and `account/route.ts:127` (deferred). CI: Vercel deploy passed twice (initial + fixup).
**Follow-ups:** Account-route migration to the service (deferred, low-risk). Anonymized rows still surface in `village.GET` adult lists as `[deleted]` — UX cleanup, defer to a UI batch. Theme A continuation L1 (`/api/village/invite-family/accept` anonymous GET-mutation) is independent and unblocked.

### 2026-05-02 · #45 · B2 — Village authz + invite role allowlist + notification per-household scoping
**Branch:** `fix/launch-b2-village-authz` → main (`130c83b`)
**Plan:** [docs/plans/launch-audit-fix-batch-02-village-authz.md](docs/plans/launch-audit-fix-batch-02-village-authz.md)
**What shipped:** Closes synthesis L2, L3, L5. Village POST/DELETE migrated to `requireHouseholdAdmin()`. Caregiver self-removal split out to `POST /api/village/leave` (non-admin, scoped to `(user.id, household.id)`) — gives L9 a clean home for the tombstone service. Invite POST gated by admin AND validates `role ∈ {parent,caregiver}` / `villageGroup ∈ {covey,field}` against an explicit allowlist *before* any Clerk metadata write, closing the bleed-back path through `requireHousehold()`'s first-user provisioning. Notifications GET/PATCH bind to active household only — multi-household users no longer have prefs silently flipped across siblings.
**Verification:** [tests/auth-access-village-authz.test.ts](tests/auth-access-village-authz.test.ts) — 18 cases. Stage 2 code-review (fresh context) pressure-tested allowlist enforcement order, notifications WHERE narrowing, ScreenCircle UI swap, and admin-gate completeness via `grep -rn "isAdmin|requireHousehold|user.role" app/api/`; all passed with file:line evidence. Companion PR #46 repointed two stale `ScreenVillage.tsx` parser tests to `ScreenCircle.tsx`, restoring full suite to 18/18 files / 156/156 tests. CI: Vercel deploy passed.
**Follow-ups:** B3 (L9 member tombstone service in `lib/services/`, called from village/leave + village DELETE + members/[id] DELETE) — unblocked. Latent observation surfaced during Stage 2: admin village-DELETE (`app/api/village/route.ts:98`) hard-deletes the DB row but never drops the Clerk org membership, while the parallel `members/[id]/route.ts:50-60` does. Worth folding into B3's scope.
